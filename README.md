# Instalation

Install `splat-transform` from npm:

```bash
npm install -g @playcanvas/splat-transform
```

Install / run local package with uv

# running 

for help run

uv run python src/compression_round_tripping/main.py --help

